create table spec_t_b_map (
	spec_t varchar,
	spec_b varchar
	);

create table spec_t_composition (
	spec_t varchar,
	constituents varchar,
	x varchar,
	category varchar,
	factor varchar,
	spec_dist_total varchar,	
	dist_pct varchar,
	market_rate varchar,
	density varchar,
	value varchar
);
